# edx-vagrant-devstack
## Install
```
mkdir devstack
cd devstack
wget https://raw.githubusercontent.com/maximst/edx-vagrant-devstack/master/Vagrantfile
vagrant plugin install vagrant-vbguest
vagrant up --provider virtualbox
```
